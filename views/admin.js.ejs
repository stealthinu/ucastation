var port = '<%= port %>';
var password = 'pass';
var socket = new io.Socket( null, { port: port } );
socket.connect();

socket.on( 'connect', function() {
  console.log( "connect." );
});

socket.send( 'admin ' + password );

var i = 0;
var url_list = new Array(2);
url_list[0] = 'drivestream';
url_list[1] = 'ぬこのこ';

function send_url() {
  socket.send( url_list[i] );
  i++;
}
